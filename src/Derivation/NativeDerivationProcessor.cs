namespace Derivation
{
    public class NativeDerivationProcessor : Processor
    {
        public NativeDerivationProcessor()
        {
        }

        public override void Execute(IModel model)
        {
            //Load dynamically derivation objects using marker object
            //sort it based on dependency json
            //execute the derivation objects
            throw new System.NotImplementedException();
        }
    }
}